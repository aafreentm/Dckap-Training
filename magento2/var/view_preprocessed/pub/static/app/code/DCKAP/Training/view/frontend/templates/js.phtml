<?php $objectManager = \Magento\Framework\App\ObjectManager::getInstance(); $store = $objectManager->get('Magento\Store\Model\StoreManagerInterface')->getStore(); $getstatesOptions = $store->getConfig('states_section/group_customstates/states_required'); $states = array(); $mystates = explode(",",$getstatesOptions); foreach($mystates as $states_id) { if($states_id) { /*$region[] = $objectManager->create('Magento\Directory\Model\ResourceModel\Region\Collection') ->addFieldToFilter('region_id', ['eq' => $states_id]) ->getFirstItem();*/ $region = $objectManager->create('Magento\Directory\Model\Region') ->load($states_id); $states[$states_id] = $region->getName(); } } #echo '<pre>';print_r($states);exit; #echo '<pre>';print_r($getstatesOptions);exit; ?> <script type="text/javascript">
  window.valuesConfig = '<?php echo $states; ?>';</script>